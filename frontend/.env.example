# API base URL (no trailing slash). Required for GitHub Pages / production.
# Example: https://your-backend.onrender.com
VITE_API_URL=http://localhost:5000

# Set in CI for GitHub Pages (e.g. /GigFlow2.0/). Leave empty for local dev.
# VITE_BASE_URL=/GigFlow2.0/
